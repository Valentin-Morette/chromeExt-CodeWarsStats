{
	"name": "chrome-extension",
	"version": "1.0.0",
	"description": "",
	"main": "background.js",
	"scripts": {
		"start": "npm init && npm i jquery",
		"push": "git add . && git commit -m 'script push' && git push origin main"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"jquery": "^3.6.3"
	}
}
